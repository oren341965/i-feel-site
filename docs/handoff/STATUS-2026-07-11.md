# סטטוס העברה — 2026-07-11 (מהמחשב הביתי של אורן, משתמש salee)

מסמך זה נכתב ע"י Claude Code במחשב הביתי, לקראת המשך עבודה מהמחשב בעבודה.

## מצב נוכחי — מאומת

- **הריפו הזה (`oren341965/i-feel-site`) הוא המקור הקנוני** של האתר.
- **האתר החי מסונכרן עם HEAD** נכון ל-2026-07-11: אומת שהתמונה מהקומיט האחרון
  (`e9933f1`, החלפת תמונת כותרת במאמר מערכת אזעקה) מוגשת באוויר (142,105 בייטים, זהה לריפו).
- **אחסון:** JetServer cPanel (שרת LiteSpeed), host `185.56.74.12`, יעד `public_html/`,
  משתמש FTP `ifeelco`. **אין Netlify** — כל אזכור של Netlify במסמכים בתיקייה הזו מיושן.
- **דיפלוי:** ידני בלבד דרך הסקיל `deploy-ifeel` (build → בדיקות → FTP כירורגי → אימות).
  GitHub Actions לא מעלה לשרת (חסום), רק בודק build.

## מה יש בתיקייה הזו

| קובץ | מה זה | סטטוס |
|---|---|---|
| `UPDATE-SPEC-2026-06.md` | מפרט 9 עדכוני האתר מיוני 2026 | חלקו יושם; אזכורי Netlify/‏Netlify Forms מיושנים — לבדוק מול הקוד לפני יישום סעיף |
| `HANDOFF-BRIEF-2026-06.md` | בריף ההעברה המקורי (העלאה לגיטהאב + Netlify) | היסטורי בלבד — שלב הגיטהאב בוצע, שלב ה-Netlify בוטל |
| `STATUS-2026-07-11.md` | המסמך הזה | עדכני |

## הערות למחשב העבודה

- במחשב הביתי (salee) יש clone עובד ב-`C:\Users\salee\Documents\i-feel-site` עם הרשאות
  push (משתמש GitHub‏ `oren341965`). אין שם סיסמת FTP — דיפלוי לא אפשרי משם כרגע.
- לפני כל עבודה: `git pull` (שני מחשבים עובדים על אותו ריפו).
- ה-tag ‏`live` (סימון הקומיט האחרון שהועלה לשרת) הוא מקומי למחשב העבודה ולא נדחף לגיטהאב —
  שווה לדחוף אותו (`git push origin live -f`) כדי ששני המחשבים יראו את נקודת הייחוס.
