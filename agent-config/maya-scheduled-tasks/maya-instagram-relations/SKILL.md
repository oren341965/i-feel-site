---
name: maya-instagram-relations
description: Run one bounded read-only monthly Monday, Instagram, and Facebook relationship review for Maya.
---

On every registered scheduler invocation, read `%USERPROFILE%\.codex\skills\maya-instagram-relations\SKILL.md` completely, then apply this stricter scheduled-run contract.

## Monthly report-only contract

- Run only on the first Sunday of each month at 10:00 in `Asia/Jerusalem`, with a 30-minute hard timeout and no overlap. If the same Israeli calendar month already has a successful report, return `COMPLETED_NO_ACTION` without opening profiles.
- Verify read-only Monday access plus Maya's exact approved I Feel Instagram and Facebook identities and the approved professional watchlist. Otherwise return the bounded blocker from the canonical skill.
- Require the current program brief and watchlist version governed by `ai-marketing-manager`, plus the reconciled roster evidence supplied by `ai-sales-manager`. Maya must not change either during the run.
- Read `references/monday-social-discovery.md`, reconcile the complete professional-roster scope and project evidence, then review only eligible public content since the last successful monthly checkpoint, with no more than a 40-day lookback. Do not browse suggested accounts or add recipients.
- Return at most one consolidated proposed direct message per professional across both platforms. Apply project-state wording, the monthly cooldown, two-unanswered-attempt ceiling, opt-out, cross-channel duplicate and recent-thread checks.
- Proposed wording may appear in the bounded scheduler result for human approval. Do not create a platform draft or perform any external write.

## Absolute prohibitions

Do not send a direct message; post a public comment; like, react, save, share, follow or unfollow; change Instagram or Facebook settings; inspect or map a private personal profile; contact an unapproved account; alter the program brief, watchlist policy or installed skill; write Monday, Gmail, WhatsApp, contacts, the Vault or the Bus; or activate a scheduler. Do not claim that an approval was granted merely because this task ran.

Return the canonical monthly result and always include `PROGRAM_OWNER=ai-marketing-manager`, Monday/profile/project-state reconciliation counts, `EXTERNAL_ACTIONS=0`, `MONDAY_WRITES=0`, `SENDS=0`, `COMMENTS=0`, `REACTIONS=0`, `FOLLOWS=0`, `NEXT_ELIGIBLE_MONTH`, and bounded blocker codes.
